name: Deploy Documentation to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'
      - 'hugo.toml'
      - '.github/workflows/pages.yml'
  workflow_dispatch:  # Allow manual triggering

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history for proper git operations
      
      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: 'latest'
          extended: true
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Initialize and install Hugo modules
        run: |
          # Initialize Hugo modules if not already initialized
          if [ ! -f "go.mod" ]; then
            hugo mod init github.com/kefer-astrology/function-wrapper
          fi
          # Download and update modules
          hugo mod get -u github.com/nanxiaobei/hugo-paper
          hugo mod tidy
          # List modules to verify
          hugo mod list
      
      - name: Build with Hugo
        run: |
          hugo --minify --destination public
        env:
          HUGO_ENVIRONMENT: production
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
